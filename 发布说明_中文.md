# 开源发布说明

推荐 GitHub 仓库名：`OrderFuse-PAM`

建议仓库简介：

> Preview evaluation utilities for order-consistent volumetric photoacoustic fusion. Full training code, data, and weights will follow after paper acceptance and permission review.

## 发布前操作

1. 新建公开仓库 `OrderFuse-PAM`，不要勾选自动生成 README、许可证或 `.gitignore`。
2. 上传本压缩包解压后的全部内容。
3. 首个标签建议命名为 `v0.1.0-preview`。
4. Release 标题建议使用 `Pre-acceptance metrics preview`。
5. 不要上传工作区中的原始数据、权重、结果目录、论文投稿PDF或包含本机路径的日志。
6. 论文录用后再发布 `v1.0.0`，并补充模型、训练、推理、权重和数据访问说明。

## 推荐项目名排序

1. **OrderFuse-PAM**：最清楚，和论文核心贡献直接对应。
2. **ConsistentPAM-Fusion**：更正式，但略长。
3. **SymmFuse3D-PAM**：强调对称与三维，但不如第一个自然。

不建议使用过于宽泛的 `PAM-Fusion`，也不建议把尚未公开的数据集名称写入仓库名。

